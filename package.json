{
  "type": "module",
  "scripts": {
    "dev": "npm run types && vite",
    "build": "vite build",
    "build:minify": "npm run build && esbuild dist/static/client.js dist/static/style.css --minify --outdir=dist/static --allow-overwrite",
    "types": "wrangler types",
    "preview": "npm run types && npm run build && wrangler dev",
    "deploy": "npm run types && npm run build:minify && wrangler deploy",
    "deploy:preview": "npm run types && npm run build:minify && wrangler deploy --env=preview"
  },
  "dependencies": {
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "@hono/vite-cloudflare-pages": "^0.4.3",
    "@hono/vite-dev-server": "^0.26.0",
    "@types/node": "^25.9.1",
    "sharp": "^0.34.5",
    "vite": "^8.0.14",
    "wrangler": "^4.95.0"
  }
}
